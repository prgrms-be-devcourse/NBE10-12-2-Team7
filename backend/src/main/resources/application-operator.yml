# full(도커) 스택 전용 — 로컬 운영 패리티. DB는 compose가 DB_URL 주입.
# actuator prometheus 노출은 application.yml(base)에서 상속 → 유지됨.
spring:
  jpa:
    properties:
      hibernate:
        format_sql: false
logging:
  level:
    root: info
    org.hibernate.SQL: warn