# demo: 더미 데이터 주입 전용 프로파일. dev 와 함께 활성화한다.
#   예) --spring.profiles.active=dev,demo
# 시작할 때마다 스키마를 drop+create 하므로 항상 깨끗한 상태에서 재시딩된다.
# ⚠️ prod 에는 절대 함께 활성화하지 말 것 (데이터 전체 소실).
spring:
  jpa:
    hibernate:
      ddl-auto: create
app:
  seed:
    demo: true
