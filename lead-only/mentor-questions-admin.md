# 멘토 질문 리스트 — Admin 도메인 (백엔드)

> 각 질문은 **"내 코드 상황 → 걱정 → 실무에선?"** 형태. 코드 위치를 같이 보여주며 물으면 좋다.

## 1. 목록을 한 번에 다 불러오기 (페이징)
- **위치**: `AdminMemberService.getMembers()` 등 → `repository.findAll()`
- **질문**: 관리자 목록을 지금은 전부 한 번에 불러오는데, 데이터가 많아지면 20개씩 끊어 보여주는 걸 넣어야 할 것 같아요. 보통 몇 건쯤부터 신경 쓰나요? 응답 형식은 어떻게 잡나요?
- **멘토 답변**:

## 2. 대시보드가 열 때마다 다시 세기 (캐싱)
- **위치**: `AdminDashboardService.getDashboard()` → `count()` × 5
- **질문**: 대시보드 열 때마다 건수를 다시 세는데, 잠깐 저장해두고 재사용하는 게 나을까요? 한다면 어떤 기준으로 하나요?
- **멘토 답변**:

## 3. 관리자 비밀번호 하드코딩
- **위치**: `AdminAccountInitializer` → `ADMIN_RAW_PASSWORD = "admin1234!"`
- **질문**: 관리자 비밀번호를 코드에 직접 적었는데, 실무에선 이런 민감한 정보를 어디에 두나요?
- **멘토 답변**:

## 4. 누가 무엇을 했는지 기록 없음 (감사 로그)
- **위치**: `AdminMemberService.changeMemberStatus()` 등 → `member.changeStatus(...)`
- **질문**: 관리자가 회원 상태를 바꾸는 기능을 만들었는데, 누가 언제 무엇을 바꿨는지 기록을 남겨야 할까요? 남긴다면 어떻게 하나요?
- **멘토 답변**:

## 5. 잘못된 상태로도 변경 가능 (상태 전이)
- **위치**: `Report.changeStatus()` → `this.status = status;`
- **질문**: 신고 상태를 아무 값으로나 바꿀 수 있게 했는데, 되돌리면 안 되는 변경(완료된 걸 되돌리기)을 막아야 하나요?
- **멘토 답변**:
