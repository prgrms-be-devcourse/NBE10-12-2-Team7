# 2026 W31 주간 로그 (7/27 ~ 8/2)

> 이번 주에 완료·진행된 작업 한 줄 요약. 문서 id를 반드시 링크로.

## 완료
- [FEAT-230](../features/FEAT-230-escrow.md) 안심결제(에스크로) 거래 v0 — 토스 단순결제 폐기 후 당근식 에스크로로 전환. 단일 `Escrow` 애그리거트 + 상태 3개(`IN_ESCROW→DONE/CANCELED`), API 4개(`/api/escrows`), 실송금은 라이선스 문제로 상태 전이 모사. 단위 6 + E2E 8 통과 (jomin4)

## 진행 중
- [FEAT-231](../features/FEAT-231-mobile-phase1.md) 모바일 Android Phase 1 — 로그인·홈목록·상세·채팅 4화면. Kotlin 7→102파일, 테스트 395개(JVM 331 + 에뮬레이터 계기 64) 전부 통과. 백엔드 API 계약을 원본 대조로 확정한 뒤 구현했고(상품 응답에 시간 필드 없음·찜 여부 미노출·`price` BigDecimal 스케일 불일치 등 지뢰 12건 사전 파악), 테스트가 제품 버그 9건을 잡았다. 최고 심각도는 `encodeDefaults` 누락으로 `autoLogin`이 요청 본문에서 통째로 생략되던 것(실제 백엔드로 재현·수정 확인). PR 대기 중 (jomin4)

## 논의 / 결정
- C2C 결제 방향: PG 단순결제는 플랫폼 가맹점 매입일 뿐 판매자 정산은 전자금융업 라이선스 필요 → v0는 상태 전이로 모사(가정) — 서사는 [FEAT-230](../features/FEAT-230-escrow.md)
- develop-workflow 스킬 개선: 구현 단계 시각화+라인설명 필수화, 단위테스트=로직 후보 제시 후 선택, ship 순서(push→PR→PR번호로 wiki→merge)·정리(워크트리 유지+브랜치만 삭제)·push 전 develop 흡수는 merge 채택
- mobile-workflow 스킬도 같은 순서로 재구성(6→7단계): ⑤ship-open(흡수→테스트→push→PR) ⑥wiki(PR번호로 문서) ⑦ship-merge. **develop 흡수를 테스트보다 먼저** 두어, 남의 코드를 삼킨 상태를 검증하고 push하도록 했다(흡수 후 미검증 push 방지). git 표에 `merge`(S1, 흡수 전용)·`gh pr edit`(S3b, 본문만)·`gh pr view/checks`(R3) 추가, `worktree remove`는 금지 목록으로 이동

## 다음 주 계획
- 에스크로 성장 경로 ①DONE→CONFIRMED+SETTLED 분리 / ②자동확정 스케줄러(72h) — [FEAT-230](../features/FEAT-230-escrow.md)
- FE 상품상세 '결제하기' 버튼·'판매완료' 연동 (frontend 워크트리)
- 모바일 Phase 2 + CI 연결(현재 `.github/workflows`에 모바일 잡 없음) — [FEAT-231](../features/FEAT-231-mobile-phase1.md)
